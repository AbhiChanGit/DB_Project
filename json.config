{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "checkJs": true,            // ← enable type-style checks in .js
    "baseUrl": ".",             // ← allow absolute imports from project root
    "paths": {
      "*": ["node_modules/*"]   // ← resolve modules from node_modules
    }
  },
  "exclude": ["node_modules"]
}

// to use this file use // @ts-check